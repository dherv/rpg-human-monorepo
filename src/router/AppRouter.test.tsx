import { render } from '@testing-library/react';
import { Router } from './AppRouter';

describe("Router component", () => {
  const baseProps = {};
  beforeEach(() => render(<Router {...baseProps} />));

  describe("props and state", () => {
    it("should", () => {});
  });

  describe("integration", () => {
    it("should", () => {});
  });
});
